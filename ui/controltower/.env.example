# Copy this file to .env (same folder) and fill in your real SAP logon.
# .env is gitignored - never commit it. ui5-deploy.yaml reads these two
# names directly (env:ABAP_USER / env:ABAP_PASSWORD) - don't rename them.

ABAP_USER=YOURSAPUSER
ABAP_PASSWORD=yourpassword
